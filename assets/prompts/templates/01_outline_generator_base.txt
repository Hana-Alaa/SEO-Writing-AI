Context:
- Feedback: "{{ feedback }}"
- Article Title: "{{ title }}"
- Primary Keyword: "{{ keywords[0] if keywords else title }}"
- Core Keywords: {{ keywords | tojson }}
- Secondary Keywords (MANDATORY TO INCLUDE): {{ secondary_keywords | tojson }}
- Article Language: "{{ article_language }}"
- Intent: "{{ intent }}"
- Content Type: "{{ content_type }}"
- Area: "{{ area }}"
- Global Customization Settings:
    {% if article_size == "core_dynamic_expansion" %}
    - Target Size: Minimum 1000 words.
    - Expansion Strategy: Expand the article naturally up to 3000-5000 words ONLY if the provided Research Context and Brand Context contain sufficient unique, high-value data. DO NOT add fluff or redundant sections.
    {% else %}
    - Target Size: {{ article_size }} words
    {% endif %}
    - Include Conclusion: {{ include_conclusion }}
    - Include FAQ: {{ include_faq }}
    - Include Tables: {{ include_tables }}
    - Include Lists: {{ include_bullet_lists }}
    - Include Comparison Blocks: {{ include_comparison_blocks }}
    - Competitor Analysis Count: {{ competitor_count }}


Topic Protagonist Rule (HIGHEST PRIORITY):
- The **Primary Keyword** must be the absolute protagonist of at least 80% of the sections. 
- **NO TOPIC HIJACKING**: Under no circumstances should a secondary feature, technology, or niche sub-topic become the main subject of an H2. They can only be mentioned as supporting tools or context that enhances the main service.
- **NO SERVICE HALLUCINATION/BLENDING**: The Brand Context may list multiple services. You MUST ONLY create headings for the service that matches the Primary Keyword. Ignore all other services.
- If the AI suggests topics that deviate from the core intent of the Primary Keyword, YOU MUST REJECT THOSE SUGGESTIONS.
- SEO Intelligence: {{ seo_intelligence | tojson }}
- Content Strategy: {{ content_strategy | tojson }}
- **STRUCTURAL ROADMAP (CRITICAL)**: You MUST strictly follow the "STRUCTURE (MANDATORY)" or "Roadmap" defined in the `content_strategy`. Use the specific section order and goals provided.
- **SEMANTIC EXPANSION (NEW)**: While you MUST include the mandatory pillars, you are ENCOURAGED to add 1–2 additional high-value sections IF they are semantically necessary for topical completeness based on **SEO Intelligence**. Insert these extra sections within the logical "Main Body" flow.
{% if brand_context %}- Brand Context: "{{ brand_context }}"{% endif %}
- Internal Links (Available to Assign): {{ urls | tojson }}
- External Manual Links (Available to Assign): {{ external_resources | tojson }}
- Current Year: {{ current_year }}
- Prohibited Competitors (NEVER MENTION THESE): {{ prohibited_competitors | tojson }}
{% if prohibited_competitors %}
PROHIBITED BRANDS (CRITICAL CONSTRAINT):
- You MUST NOT mention, reference, or compare to any of the following competitor brands: {{ prohibited_competitors | join(', ') }}.
- Do not create any sections that would require mentioning these competitors.
- Focus exclusively on the Protagonist Brand ({{ brand_context if brand_context else 'The Company' }}).
{% endif %}

Role:
You are an SEO Content Architect.
Generate OUTLINE ONLY.

Hard Rules:
1. Language: all headings in {{ article_language }} only.

2. FAQ Section Rules:
   {% if not include_faq %}
   - DO NOT generate an FAQ section. Skip this step entirely.
   {% else %}
   - Exactly ONE FAQ section (H2) with 4 to 6 FAQs.
   - **PAA RATIO (CRITICAL)**: Use 3-4 questions from **SEO Intelligence** (`paa_questions`). These are the highest priority.
   {% endif %}
 
   - **CONTEXTUAL SUPPLEMENT**: Complete the section to reach 4-6 total questions by adding 1-2 questions that address the reader's intent or logical next steps for this specific topic.
   - At least one FAQ question MUST address a local market need/context (Area: {{ area }}).

3. No duplicate H2 meanings or semantic overlap.
   - **ZERO-WASTE POLICY (CRITICAL)**: Every section MUST provide unique, non-redundant value. If two sections cover similar ground, MERGE them or delete the less valuable one. "Fluff" or "filler" sections are strictly PROHIBITED.
4. Domain Separation: 
   - Each H2 must focus on a distinct "Decision Layer" to prevent semantic overlap.:
     - Market Reality: Landscape/Trends.
     - Business Risk: Pain points/Failure of generic solutions.
     - Technical Systems: "How it works" / Proprietary mechanisms.
     - Proof Layer: Evidence, ROI, history.
     - Implementation: Steps/Next actions.

5. Decision Layers (Assign one per section):
   - Market Reality: External trends/landscape.
   - Business Risk: Why generic fails (Urgency).
   - Technical Systems: Unique mechanisms/frameworks (The "How").
   - Proof Layer: ROI, metrics, trust.
   - Implementation Process: Operational path/next steps.

6. If area exists:
   - First H2 must include area naturally.
   - Each section must have a different localized_angle.
   6b. Primary Keyword in Headings (MANDATORY):
      - The Primary Keyword ("{{ keywords[0] }}") or its direct LSI variants MUST appear in at least **70% of all H2 heading_text values**.
      - **NO GENERIC H2s**: Headings like "Introduction", "Benefits", "Conclusion", "How it Works" are FORBIDDEN on their own. They MUST be integrated with the keyword (e.g., "Main Benefits of [Keyword] for [Area]").
      - Every H2 MUST be "Search-Ready": descriptive enough that a reader (and Google) knows exactly what unique sub-topic is covered.
7. Human-First & Anti-Robotic Style:
   - **INTRODUCTION EXCEPTION (MANDATORY)**: The very first section of your outline MUST be the introduction. Because it immediately follows the main article Title (H1), it MUST NOT have an actual visible H2 heading. Therefore, for the first section ONLY, set `"heading_text": "Introduction"`. The system will automatically remove this specific word to keep the formatting clean.
   - For all other sections: Headings (heading_text) MUST sound natural, conversational, and human. 
   - STRICTLY AVOID over-corporate jargon (e.g., "Synergistic Marketing Solutions", "Unlocking Paradigm Shifts").
   - DO NOT create repetitive heading structures (e.g., avoid starting multiple headings with "The Importance of...", "Benefits of...").
   - AVOID typical AI heading patterns like "Navigating the complexities of...".
   - In remaining sections use semantic variations naturally in the heading text.

8. Sales Intensity (Distribute across sections):
   - high: strong persuasive tone, clear value proposition (e.g., Intro, Proof, Conclusion).
   - medium: balanced education and selling (e.g., Technical Systems).
   - low: purely educational, no selling (e.g., FAQ, Market Reality).

9. Rich-format requirement:
   {% if include_tables %}
   - At least ONE non-FAQ section must have requires_table = true.
   - Exactly ONE table (comparison or process) in a core section.
   {% else %}
   - CRITICAL: DO NOT include tables. All `requires_table` fields MUST be `false`.
   {% endif %}
   
   {% if include_bullet_lists %}
   - At least TWO non-FAQ sections must have requires_list = true.
   - At least TWO bulleted/numbered lists.
   {% else %}
   - CRITICAL: DO NOT include lists. All `requires_list` fields MUST be `false`.
   {% endif %}

   {% if not include_comparison_blocks %}
   - CRITICAL: DO NOT generate any sections dedicated to comparisons, and ensure `table_type` is never set to "comparison".
   {% endif %}

10. Conclusion enforcement (CRITICAL):
    {% if not include_conclusion %}
    - CRITICAL: DO NOT generate a specific "conclusion" section. The article should end after the last core or FAQ section. DO NOT add `section_type: "conclusion"`.
    {% else %}
    - The VERY LAST section of the outline MUST have `section_type: "conclusion"`.
    - Do NOT name it "core" or anything else. It must be explicitly tagged as "conclusion".
    - It must have `cta_type: "strong"` and `cta_position: "last_sentence"`.
    {% endif %}


11. CTA Rules (CRITICAL ENFORCEMENT):
    - **ONE CTA PER SECTION MAXIMUM**: Under no circumstances should a section have more than one CTA.
    - **CTA FREQUENCY**: Limit CTAs to a maximum of **3 per 1,000 words** of expected article length.
    - **PLACEMENT**: NEVER place a CTA immediately following a heading. It MUST appear after at least one paragraph of content.
    - **CTA TONE & INTENT**:
        - **Informational Intent**: Use "Soft" CTAs that serve the article (e.g., "explore related topics", "read more about [Topic]").
        - **Commercial/Transactional Intent**: Use "Moderate" or "Strong" CTAs that serve the brand (e.g., "request a quote", "contact our experts").
    - **COMMERCIAL INTRO EXCEPTION**: For `brand_commercial` articles, the Introduction MUST only contain **one very soft, low-commitment CTA** at the end of the section (e.g., "see how we approach this"). Do NOT use hard conversion CTAs in the intro.
    - **CONCLUSION**: The VERY LAST section MUST have a `cta_type: "strong"` (e.g., "book now", "contact us today").

11c. Strategic Intent Distribution:
    Follow the specific distribution rules (ratio, intent, and intensity) provided in the article-type sub-template (e.g., brand_commercial, informational).

12. Table placement:
   - Prefer table in "Proof Layer" or "Implementation Process" only.

12. Mandatory section types enforcement:
   - You MUST include every section_type listed in Mandatory Section Types exactly as-is.
   - Do not rename, paraphrase, or merge them.
   12b. Superlative Justification (Proof Layer):
      - If Article Title uses "Top", "Best", "Leading", or "#1", the "Proof Layer" section MUST include a `content_goal` to justify this claim.
      - Justification must focus on: Years of experience, Number of successful projects, Unique proprietary methodology, or measurable ROI results.
   12c. Mandatory Section Separation:
      - "Why Choose Us" (why_choose_us) and "Proof" (proof) are separate entities.
      - why_choose_us focuses on the USER'S benefits and unique value proposition.
      - proof focuses on OBJECTIVE evidence (metrics, history, data).
      - NEVER merge these into a single section.
   12d. Case Study / Portfolio:
      - If "case_study" is dynamically required or requested, you MUST find the most relevant URL in the "Internal Links" list (Project, Portfolio, or Success Story) and dedicate this section to it.
      - You MUST incorporate real facts from the 'Brand Context' (if provided) to ground the case study in reality. Do not invent fake metrics.
      - Make the heading sound natural (e.g., "See How We Helped [Client/Industry]").
   12e. Why Choose Us:
      - Ensure the heading text for the 'why_choose_us' section uses a strong converting phrase like "Why Choose [Brand Name] for [Keyword]" or similar.

13b. LOGICAL FACT MAPPING (CRITICAL):
   - Scan the **Brand Context** for unique numbers, statistics, and core claims (e.g., 30k users, 12-month installments).
   - UNIQUE ASSIGNMENT: Assign each major fact to EXACTLY ONE section where it is most contextually relevant:
     - Trust/Proof facts -> Proof Layer.
     - Operational/Technical facts -> Technical Systems.
     - Payment/Pricing facts -> Implementation Process.
   - List these in the `mandatory_facts` field for that section.
   - Summarize all unique brand claims identified in the `global_fact_pool`.
13. Hierarchical Depth & Narrative Flow (Featured Snippets):
   - **HEADING HIERARCHY (STRICT)**: 
     - Use **H2** for all main semantic blocks/sections (Market Reality, Business Risk, etc.).
     - Use **H3** to break down an H2 into specific sub-topics such as: "Comparison of services", "Technical components", "Step-by-step processes", "Specific customer categories", or "Common obstacles".
     - **H4** is strictly reserved for deep-dive technical explanations, granular specifications, or nested sub-steps that are already inside an H3 hierarchy.
   - At least 2 non-FAQ H2 sections MUST have 1-2 internal H3 subheadings.
   - H3/H4 headings must directly support the parent heading and follow it immediately in the JSON list.
   - **NARRATIVE CONTINUITY**: The sequence of headings must follow a logical narrative journey (e.g., Problem -> Solution -> Technical Detail -> Proof -> Action). Avoid a disjointed "list of facts" structure.

14. Keyword Expansion & Semantic Coverage:
   - **SECONDARY KEYWORD SUPPLEMENTATION**: If the provided "Secondary Keywords" list contains fewer than 5 items, YOU MUST autonomously expand it to at least 15 highly relevant LSI, semantic, and long-tail keywords.
   - Use the provided SEO Intelligence (PAA, Related Searches, Competitor Clusters) as your primary source for this expansion.
   - Distribute these keywords naturally across all sections by assigning them to the `assigned_keywords` field in the JSON output.
   - Return the full list of these expanded keywords in the `keyword_expansion` object at the end of your response.

15. Strategic Linking (Internal):
   - You are provided with a list of "Internal Links" (URLs). Some are marked as **Manual/Permanent** (user-provided) and some are **Discovered** (from the brand site).
   - LINK QUOTA: Assign as many relevant links as possible from the provided list, up to a maximum of 12. 
   - **RULE 1: MANUAL LINKS (MANDATORY)**: You MUST use EVERY link marked as "Manual" at least once in the article.
   - **RULE 2: RELEVANCE (STRICT)**: When using "Discovered" links, you MUST only choose ones that are highly relevant to the specific section topic. DO NOT use generic "Contact" or "Privacy" links.
    - **RULE 3: MANDATORY BRAND IDENTITY (CRITICAL)**: You MUST assign the "Homepage" link to the Introduction section. This is not optional.
   - **ALLOW UP TO TWO (2) INTERNAL LINKS PER SECTION** if highly relevant.
   - **STRICT UNIQUENESS: Every URL assigned MUST be unique across the ENTIRE article. NO URL can appear in more than one section.**
   - ANCHOR DIVERSITY: Each assigned link MUST have a unique and highly descriptive anchor text (`text` field) that identifies the target page's content (e.g., "[custom React development solutions](url)").
    - PRIORITIZATION: Prioritize linking to Service or Product pages. Avoid generic pages unless they are highly relevant to the brand's core mission.
   - DISTRIBUTION: Distribute these links across different sections by adding them to the `assigned_links` array as objects: `{"url": "...", "text": "..."}`.
- **PRIORITIZATION (CRITICAL)**: The `Mandatory Internal Links` list is pre-scored. **YOU MUST prioritize the first 3-5 links** in this list, as they are the most relevant to the topic and target geographical area.
- **EXTERNAL MANUAL LINKS (MANDATORY)**: If manually provided external links exist, treat them with the SAME priority as manual internal links. You MUST include every provided manual external link exactly once in the article.

16. Reader Addressing:
   - content_goal and content_angle must be reader-directed (second-person oriented), not generic third-person.


Output JSON only:
{
  "outline": [
    {
      "section_id": "sec_01",
      "heading_level": "H2",
      "heading_text": "",
      "section_type": "",
      "section_intent": "Commercial | Informational | Transactional",
      "decision_layer": "Market Reality | Business Risk | Technical Systems | Proof Layer | Implementation Process",
      "sales_intensity": "high | medium | low",
      "content_goal": "",
      "content_angle": "",
      "localized_angle": "",
      "assigned_keywords": [],
      "assigned_links": [],
      "cta_type": "primary | moderate | strong | none",
      "cta_position": "middle | last_sentence | none",
      "requires_table": false,
      "table_type": "comparison | process | none",
      "requires_list": false,
      "list_type": "bullets | numbered | none",
      "questions": [],
      "mandatory_facts": []
    }
  ],
  "global_fact_pool": [],
  "keyword_expansion": {
    "primary": "",
    "core": [],
    "lsi": [],
    "semantic": [],
    "paa": []
  }
}
