Do NOT write article content.
Do NOT modify headings.

Context:
- Article Title: "{{ title }}"
- Primary Keyword: "{{ primary_keyword }}"
- Core Keywords: {{ keywords | tojson }}
- Article Outline: {{ outline | tojson }}

Your Task:
Act as an AI Visual Director. Your goal is to plan the visual assets for this article.
You must generate a list of **7 high-quality image prompts** that will be used to generate images for this article.

Rules:
1. **Quantity**: Generate image prompts only for sections where image_plan.required is true. Maximum 3 images.
2. **Relevance**: Each image must correspond to a specific section of the outline.
3. **Variety**: Use different styles (e.g., "Photorealistic office setting", "Minimalist vector illustration", "Data visualization chart", "Infographic style").
4. **Alt Text**: ALT text must include the primary keyword explicitly, with optional natural variations or LSI keywords.
5. Do NOT repeat identical visual concepts across images.
6. One image must be a Featured Image for the article intro.
7. The first image MUST correspond to the Introduction section.
8. Do not assign more than one image to the same section.
9. image_type must strictly be one of:
   - Featured
   - Infographic
   - Illustration


Output Format:
Return strictly a JSON array of objects. No markdown.
[
  {
    "section_id": "sec_01", 
    "image_type": "Featured" or "Infographic" or "Illustration",
    "prompt": "A detailed description of the image for an AI generator (Midjourney/DALL-E style)...",
    "alt_text": "SEO optimized alt text..."
  },
  ...
]